angular.module('XPBoard').directive(
  'stickyColumn', function() {
    return {
      replace: True,
      scope: {
        tickets: "=tickets"
      },
      templateUrl: 
    }
  }
).directive(
  'sticky', function() {
    return {
      replace: True,
      scope: {
        
      }
    }
  }
);
                                    
